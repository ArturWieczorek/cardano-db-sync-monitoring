# Runtime dependencies for db-sync-monitoring scripts.
# For development tooling (pytest, ruff, mypy, pre-commit), see requirements-dev.txt.
#
# Versions pinned for reproducibility. Loose pins live in pyproject.toml's
# [project.dependencies]; use `pip install .` to install from pyproject directly.

pandas==2.2.3
plotly==6.0.1
psutil==7.0.0
psycopg2-binary==2.9.10

# Transitive deps of the above (pinned for reproducibility).
narwhals==1.36.0
numpy==2.2.5
packaging==25.0
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
tzdata==2025.2
